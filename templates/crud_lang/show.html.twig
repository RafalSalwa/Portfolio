{% extends 'base.html.twig' %}

{% block title %}Apps{% endblock %}

{% block body %}
    <h1>Apps</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ lang.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ lang.name }}</td>
        </tr>
        <tr>
            <th>Img</th>
            <td>{{ lang.img }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_crud_lang_index') }}">back to list</a>

    <a href="{{ path('admin_crud_lang_edit', {'id': lang.id}) }}">edit</a>

    {{ include('crud_lang/_delete_form.html.twig') }}
{% endblock %}
