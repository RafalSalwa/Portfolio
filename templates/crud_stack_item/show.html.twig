{% extends 'base.html.twig' %}

{% block title %}StackItem{% endblock %}

{% block body %}
    <h1>StackItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stack_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stack_item.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ stack_item.url }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ stack_item.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_crud_stack_item_index') }}">back to list</a>

    <a href="{{ path('admin_crud_stack_item_edit', {'id': stack_item.id}) }}">edit</a>

    {{ include('crud_stack_item/_delete_form.html.twig') }}
{% endblock %}
