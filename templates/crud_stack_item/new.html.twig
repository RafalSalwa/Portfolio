{% extends 'admin.html.twig' %}

{% block title %}New StackItem{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-xl-12">
            <div class="card">
                <div class="row card-header">
                    <div class="col-xl-11">
                        <h1>Create new Application</h1>
                    </div>
                    <div class="col-xl-1 text-end">
                        <a href="{{ path('admin_crud_stack_item_index') }}">
                            <button type="button" class="btn btn-secondary">
                                <span class="tf-icons bx bx-arrow-back"></span>
                                back
                            </button>
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    {{ include('crud_stack_item/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
