<form method="post" action="{{ path('app_programming_language_delete', {'id': programming_language.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programming_language.id) }}">
    <button class="btn">Delete</button>
</form>
