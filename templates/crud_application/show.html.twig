{% extends 'admin.html.twig' %}

{% block title %}Application{% endblock %}

{% block body %}
    <h1>Application</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ application.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ application.name }}</td>
        </tr>
        <tr>
            <th>Apps</th>
            <td>{{ application.lang }}</td>
        </tr>
        <tr>
            <th>Slug</th>
            <td>{{ application.slug }}</td>
        </tr>
        <tr>
            <th>Img</th>
            <td>{{ application.img }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_crud_application_index') }}">back to list</a>

    <a href="{{ path('admin_crud_application_edit', {'id': application.id}) }}">edit</a>

    {{ include('crud_application/_delete_form.html.twig') }}
{% endblock %}
